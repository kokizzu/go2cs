namespace go;

using fmt = fmt_package;

partial class main_package {

internal static void Main() {
    {
        nint a = -1; if (a < 0) {
            fmt.Println((@string)"a is less than 0"u8);
        }
    }
    {
        nint a = 1; if (a > 0) {
            fmt.Println((@string)"a is greater than 0"u8);
        }
    }
    nint b = 99;
    fmt.Println((@string)"b before ="u8, b);
    {
        nint bΔ1 = -1; if (bΔ1 < 0) {
            fmt.Println((@string)"b now ="u8, bΔ1);
            fmt.Println((@string)"b is less than 0"u8);
        }
    }
    {
        nint bΔ2 = 1; if (bΔ2 > 0) {
            fmt.Println((@string)"b now ="u8, bΔ2);
            fmt.Println((@string)"b is greater than 0"u8);
        }
    }
    fmt.Println((@string)"b after ="u8, b);
}

} // end main_package
