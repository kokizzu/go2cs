{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestGamma/sub",
      "class": "alloc-count-semantics",
      "signature": "num allocations = ",
      "reason": "exact-count AllocsPerRun assert (want 1): the managed shim is byte-derived, so a nonzero count assert can never agree"
    },
    {
      "name": "TestPipeAllocations",
      "class": "deferred",
      "signature": "too many allocations for Pipe() call: ",
      "reason": "count-bound AllocsPerRun assert: the managed Pipe allocates per call what Go keeps off the heap",
      "want": "<= 4 per call",
      "reading": "6 per call (Release, tiered JIT off)",
      "plan": "docs/phase4/DESIGN-example-plan.md: the per-call allocations fold into the pipe's own state"
    }
  ]
}
