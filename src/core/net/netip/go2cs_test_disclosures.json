{
    "schemaVersion":  1,
    "disclosures":  [
                        {
                            "name":  "TestAddrStringAllocs/ipv4",
                            "class":  "deferred",
                            "signature":  ", want 1",
                            "reason":  "exact-count AllocsPerRun assert (t.Errorf 'allocs=%d, want 1'): Addr.String() must complete in exactly one allocation (the result string); the converted formatting path allocates 2 golib-site object(s) per run - the append-based hex/decimal formatting's intermediate slice storage the CLR heap-allocates where Go's escape analysis keeps it on the stack. True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the count reaches 1. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the formatting path's intermediate slice storage, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "1 allocation per run (Addr.String()'s result string)",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 2; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=2, want 1`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the append-based hex/decimal formatting buffers of Addr.String(). Retires when a run reads 1 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestAddrStringAllocs/ipv6",
                            "class":  "deferred",
                            "signature":  ", want 1",
                            "reason":  "exact-count AllocsPerRun assert (t.Errorf 'allocs=%d, want 1'): Addr.String() must complete in exactly one allocation (the result string); the converted formatting path allocates 106 golib-site object(s) per run - the append-based hex/decimal formatting's intermediate slice storage the CLR heap-allocates where Go's escape analysis keeps it on the stack. True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the count reaches 1. Measured 106 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the formatting path's intermediate slice storage, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "1 allocation per run (Addr.String()'s result string)",
                            "reading":  "106 per run -- 106,000 go2cs-runtime object allocations (11,968,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 3; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=106, want 1`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the append-based hex/decimal formatting buffers of Addr.String(). Retires when a run reads 1 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestAddrStringAllocs/ipv6+zone",
                            "class":  "deferred",
                            "signature":  ", want 1",
                            "reason":  "exact-count AllocsPerRun assert (t.Errorf 'allocs=%d, want 1'): Addr.String() must complete in exactly one allocation (the result string); the converted formatting path allocates 106 golib-site object(s) per run - the append-based hex/decimal formatting's intermediate slice storage the CLR heap-allocates where Go's escape analysis keeps it on the stack. True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the count reaches 1. Measured 106 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the formatting path's intermediate slice storage, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "1 allocation per run (Addr.String()'s result string)",
                            "reading":  "106 per run -- 106,000 go2cs-runtime object allocations (11,968,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 4; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=106, want 1`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the append-based hex/decimal formatting buffers of Addr.String(). Retires when a run reads 1 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestAddrStringAllocs/ipv4-in-ipv6",
                            "class":  "deferred",
                            "signature":  ", want 1",
                            "reason":  "exact-count AllocsPerRun assert (t.Errorf 'allocs=%d, want 1'): Addr.String() must complete in exactly one allocation (the result string); the converted formatting path allocates 3 golib-site object(s) per run - the append-based hex/decimal formatting's intermediate slice storage the CLR heap-allocates where Go's escape analysis keeps it on the stack. True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the count reaches 1. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the formatting path's intermediate slice storage, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "1 allocation per run (Addr.String()'s result string)",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (136,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 5; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=3, want 1`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the append-based hex/decimal formatting buffers of Addr.String(). Retires when a run reads 1 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestAddrStringAllocs/ipv4-in-ipv6+zone",
                            "class":  "deferred",
                            "signature":  ", want 1",
                            "reason":  "exact-count AllocsPerRun assert (t.Errorf 'allocs=%d, want 1'): Addr.String() must complete in exactly one allocation (the result string); the converted formatting path allocates 3 golib-site object(s) per run - the append-based hex/decimal formatting's intermediate slice storage the CLR heap-allocates where Go's escape analysis keeps it on the stack. True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the count reaches 1. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the formatting path's intermediate slice storage, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "1 allocation per run (Addr.String()'s result string)",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (136,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 6; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=3, want 1`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the append-based hex/decimal formatting buffers of Addr.String(). Retires when a run reads 1 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/IPv4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 1 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 1 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "1 per run -- 1,000 go2cs-runtime object allocations (64,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 7; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 1; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/AddrFrom4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 1 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 1 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "1 per run -- 1,000 go2cs-runtime object allocations (64,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 8; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 1; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/AddrFrom16",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (104,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 9; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/ParseAddr/4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 10; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/ParseAddr/6",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (112,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 11; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/MustParseAddr",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 12; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/IPv6LinkLocalAllNodes",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 13; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/IPv6LinkLocalAllRouters",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 14; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/IPv6Loopback",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 15; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsZero",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 16; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.BitLen",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 17; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Zone/4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 18; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Zone/6",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 19; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Zone/6zone",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 4 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 4 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "4 per run -- 4,002 go2cs-runtime object allocations (370,928 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 20; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 4; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Compare",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (192,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 21; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Less",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (192,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 22; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Is4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 23; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Is6",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 24; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Is4In6",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 25; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Unmap",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 26; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.WithZone",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 27; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsGlobalUnicast",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (536,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 28; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsInterfaceLocalMulticast",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (536,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 29; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsLinkLocalMulticast",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (536,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 30; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsLinkLocalUnicast",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (536,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 31; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsLoopback",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 32; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsMulticast",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 33; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsPrivate",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 6 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 6 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "6 per run -- 6,000 go2cs-runtime object allocations (536,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 34; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 6; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.IsUnspecified",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (88,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 35; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Prefix/4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 36; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Prefix/6",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 2 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "2 per run -- 2,000 go2cs-runtime object allocations (80,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 37; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 2; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.As16",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (176,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 38; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.As4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (160,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 39; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Next",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 40; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Addr.Prev",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 3 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 3 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "3 per run -- 3,000 go2cs-runtime object allocations (96,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 41; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 3; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/AddrPortFrom",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 1 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 1 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "1 per run -- 1,000 go2cs-runtime object allocations (64,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 42; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 1; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/ParseAddrPort",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (192,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 43; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/MustParseAddrPort",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (192,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 44; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/PrefixFrom",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 1 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 1 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "1 per run -- 1,000 go2cs-runtime object allocations (64,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 45; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 1; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/ParsePrefix/4",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (168,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 46; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/ParsePrefix/6",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 4 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 4 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "4 per run -- 4,000 go2cs-runtime object allocations (144,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 47; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 4; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/MustParsePrefix",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (168,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 48; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Prefix.Contains",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 8 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 8 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "8 per run -- 8,000 go2cs-runtime object allocations (264,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 49; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 8; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Prefix.Overlaps",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 10 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 10 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "10 per run -- 10,000 go2cs-runtime object allocations (336,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 50; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 10; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Prefix.IsZero",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (168,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 51; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Prefix.IsSingleIP",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (168,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 52; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestNoAllocs/Prefix.Masked",
                            "class":  "deferred",
                            "signature":  "allocs = ",
                            "reason":  "want-0 AllocsPerRun assert (t.Fatalf 'allocs = %d; want 0'): the measured closure runs netip constructors/methods that Go keeps allocation-free via stack allocation and inlining (the package's own TestInlining pins those inlining decisions), while the converted path allocates 5 golib-site object(s) per run - parse working buffers, slice headers, and pointer-box shells the managed conversion needs on the heap. go2cs's AllocsPerRun counter is a true object count over golib's allocation sites (a lower bound on the real total), so this is a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when its site's count reaches zero. Measured 5 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: parse working buffers, slice backings and pointer boxes, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run",
                            "reading":  "5 per run -- 5,000 go2cs-runtime object allocations (168,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 53; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs = 5; want 0`",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: the netip constructor/method path the measured closure runs. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestParsePrefixAllocs/192.168.1.0/24",
                            "class":  "deferred",
                            "signature":  ", want 0",
                            "reason":  "differential want-0 AllocsPerRun assert (t.Errorf 'allocs=%d, want 0'): asserts ParsePrefix allocates exactly as many objects as ParseAddr of the same address; the converted ParsePrefix adds 2 golib-site object(s) per run over ParseAddr's own count (the prefix parse's extra slicing and value box). True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the differential reaches zero. Measured +2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the prefix parse's extra slicing and value box, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run beyond ParseAddr's own count (a differential: prefixAllocs - ipAllocs)",
                            "reading":  "2 per run -- 10 go2cs-runtime object allocations (320 bytes) counted over 5 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 54; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=2, want 0`; INSTRUMENT LIMIT: the host records the unit for a test's FIRST nonzero call only, which here is the ParseAddr leg; the failing figure is the differential, read from the test's own message",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: ParsePrefix's work beyond ParseAddr. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        },
                        {
                            "name":  "TestParsePrefixAllocs/aaaa:bbbb:cccc::/24",
                            "class":  "deferred",
                            "signature":  ", want 0",
                            "reason":  "differential want-0 AllocsPerRun assert (t.Errorf 'allocs=%d, want 0'): asserts ParsePrefix allocates exactly as many objects as ParseAddr of the same address; the converted ParsePrefix adds 2 golib-site object(s) per run over ParseAddr's own count (the prefix parse's extra slicing and value box). True object count over golib's allocation sites (a lower bound on the real total): a genuine allocation-profile divergence, not a counting-regime artifact. Reduction of exactly these sites is the banked zh-box reduction arc's netip harvest; this entry retires when the differential reaches zero. Measured +2 object(s)/run, 2026-08-29, go1.23.12/windows-amd64. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the prefix parse's extra slicing and value box, the family this reason already assigns to the zh-box netip harvest.",
                            "want":  "0 allocations per run beyond ParseAddr's own count (a differential: prefixAllocs - ipAllocs)",
                            "reading":  "2 per run -- 10 go2cs-runtime object allocations (400 bytes) counted over 5 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 55; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocs=2, want 0`; INSTRUMENT LIMIT: the host records the unit for a test's FIRST nonzero call only, which here is the ParseAddr leg; the failing figure is the differential, read from the test's own message",
                            "plan":  "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's sites: ParsePrefix's work beyond ParseAddr. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
                        }
                    ]
}