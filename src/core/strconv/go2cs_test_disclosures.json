{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestAllocationsFromBytes/Atoi",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "3 per run -- 300 go2cs-runtime object allocations (11,200 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 73; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 3 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stages 2 and 3 (range []byte(s) is Atoi's and ParseUint's, strconv/atoi.cs:267 and :145), and docs/phase4/DESIGN-string-literal-allocation.md §8 arm C for the function-local const (fnAtoi). the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/ParseBool",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (3,200 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 74; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/ParseInt",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "4 per run -- 400 go2cs-runtime object allocations (15,200 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 75; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 4 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stages 2 and 3 (range []byte(s) is Atoi's and ParseUint's, strconv/atoi.cs:267 and :145), and docs/phase4/DESIGN-string-literal-allocation.md §8 arm C for the function-local const (fnParseInt and fnParseUint). the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/ParseUint",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "3 per run -- 300 go2cs-runtime object allocations (12,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 76; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 3 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stages 2 and 3 (range []byte(s) is Atoi's and ParseUint's, strconv/atoi.cs:267 and :145), and docs/phase4/DESIGN-string-literal-allocation.md §8 arm C for the function-local const (fnParseUint). the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/ParseFloat",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (4,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 77; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/ParseComplex",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (4,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 78; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/CanBackquote",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (4,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 79; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/AppendQuote",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run before the pre-sized append. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (4,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 80; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/AppendQuoteToASCII",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run before the pre-sized append. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (4,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 81; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestAllocationsFromBytes/AppendQuoteToGraphic",
      "class": "deferred",
      "signature": "allocs, want 0 allocs",
      "reason": "want-zero AllocsPerRun assert: the closure's string([]byte) argument is proven non-escaping by Go's compiler and stays on the stack; the managed model must materialize an @string per run before the pre-sized append. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string-conversion intermediates. Read at Atoi (src/core/strconv/atoi.cs:255-267): the test's string(bytes.Number) argument, the function-local `\"Atoi\"u8` constant materialised per call, and `range []byte(s)` copied through slice<byte>(s) -- three conversions Go performs without a heap object, which is Atoi's reading of 3. Only Atoi was decomposed; the other nine share the argument site.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 100 go2cs-runtime object allocations (4,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 82; the host's unit note is COUNT, a LOWER BOUND; the test printed `got 1 allocs, want 0 allocs`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 2. the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. The family's other arms, read at Atoi: a function-local string constant hoisted by docs/phase4/DESIGN-string-literal-allocation.md §8 arm C, and `range []byte(s)` walked over the string's bytes without a copy, as Go's compiler does. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    }
  ]
}
