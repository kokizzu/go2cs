{
  "schemaVersion": 1,
  "notes": [
    "Linux only: on Windows both runtimes skip TestSymVersion identically (skipIfNotELF), so the Windows row stays 10 matched / 0 disclosed and this entry never fires there."
  ],
  "disclosures": [
    {
      "name": "TestSymVersion",
      "class": "platform-skip",
      "signature": "no .gosymtab section",
      "reason": "Host identity, reached through Go's own skip. TestSymVersion -> getTable -> crack(os.Args[0]) -> parse, whose first act is `if f.Section(\".gosymtab\") == nil { t.Skip(\"no .gosymtab section\") }`: it cracks the RUNNING TEST BINARY. Go's test binary is a Go ELF with a .gosymtab section, so the Go side passes; the converted host is a .NET apphost with no Go symbol table, so the same upstream guard fires and the converted side skips with Go's own text. Nothing in the package is wrong and nothing go2cs owns can satisfy it short of shipping a Go pclntab inside a .NET executable. Banked on Linux 2026-08-29 as `linux: 9 + 1` (roster commit 73c3396de, window-33 ruling) with the signature captured from the run but no manifest committed beside the package; this file is that manifest, added 2026-09-03 when the Linux leveling re-sweep at master 22d2bd9dc read the row 9 matched + 1 unabsorbed (Go pass / C# skip, this exact text) and the roster guard gained the line that every disclosed count must be backed by a committed manifest."
    }
  ]
}
