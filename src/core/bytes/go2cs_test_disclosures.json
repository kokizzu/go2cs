{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestGrow",
      "class": "deferred",
      "signature": "allocation occurred during write",
      "reason": "want-zero AllocsPerRun assert: the converted Buffer.Grow/Write path allocates in the managed slice model where Go's pre-grown buffer writes allocation-free. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O3): alloc-profile -> deferred. REASON CORRECTED: Go's Buffer also allocates, once per doubling, and passes by truncation (buffer_test.go:549-568; go1.24.13 testing/allocs.go:40-44); the host floors the same sub-one quotient to 1 (testing.cs:755). The reason's 'Go's pre-grown buffer writes allocation-free' is not what Go measures.",
      "want": "0 per run (Go's integer quotient)",
      "reading": "1 per run -- 14 go2cs-runtime object allocations (51,528 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 91 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write | allocation occurred during write`; 19 of 25 legs fail; only the first nonzero leg is recorded; Go's integer quotient of 14/100 is 0",
      "plan": "docs/phase4/DESIGN-allocation-counting.md §9 (REC-F) (i), owner C1, the post-hop instrument seat: the COUNT arm's integer quotient with a residue guard whose precondition is that AllocationCounter tallies the BYTES it counts, so residue = (allocated - countedBytes) / runs. The guard's first form, (allocated - counted x 24 B) / runs, does NOT retire this entry (about 512 B per run of residue); under the counted-bytes rule it retires if its 14 counted growth arrays carry all 51,528 bytes -- UNMEASURED. When it lands this entry is scoped [linux, darwin] at the Windows bank and deleted only when Linux reads it passing on a post-seat tree. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestIndex",
      "class": "deferred",
      "signature": "expected no allocations, got ",
      "reason": "want-zero AllocsPerRun assert: the converted Index search allocates in the managed runtime where Go's compiler stack-allocates its working state. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: Read at src/core/internal/bytealg/bytealg.cs:82 and :90: IndexRabinKarp compares `s[..n].ToGoString() == sep.ToGoString()`, Go's `string(s[:n]) == string(sep)`, which Go compares without allocating; each side mints an @string copy, which is the reading of 2.",
      "want": "0 allocations per run",
      "reading": "2 per run -- 200 go2cs-runtime object allocations (19,200 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 92; the host's unit note is COUNT, a LOWER BOUND; the test printed `expected no allocations, got 2.000000`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 1 (dated 2026-09-23). the string-conversion family's comparison arm, whose mechanism already ships: the converter's string([]byte) elision emits golib's non-allocating `sstring` view for a provably read-only, non-escaping conversion (docs/ConversionStrategies-Reference.md, *Strings (`@string` and `sstring`)*; bytes.Equal's own body is `((sstring)a) == ((sstring)b)`, src/core/bytes/bytes.cs:25). The increment is that elision reaching a TYPE-PARAMETER operand: in the generic IndexRabinKarp[T string | []byte] the same Go comparison renders through ByteSeqExtensions.ToGoString and copies both sides. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
    },
    {
      "name": "TestIndexRune",
      "class": "deferred",
      "signature": "expected no allocations, got ",
      "reason": "want-zero AllocsPerRun assert: string(r) materializes a byte[] in the managed model where Go uses a 4-byte stack buffer (runtime.intstring). RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string(r) for a lookup, which Go builds in runtime.intstring's 4-byte stack buffer.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 1,000 go2cs-runtime object allocations (32,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 93; the host's unit note is COUNT, a LOWER BOUND; the test printed `expected no allocations, got 1.000000`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 4 (dated 2026-09-23). the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. This entry's site is the family's rune arm: a transient one-rune @string over a stack 4-byte encoding for a string(r) the callee does not retain, the managed counterpart of runtime.intstring's buffer. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
    },
    {
      "name": "TestLastIndex",
      "class": "deferred",
      "signature": "expected no allocations, got ",
      "reason": "want-zero AllocsPerRun assert: the converted LastIndex search allocates in the managed runtime where Go's compiler stack-allocates its working state. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: Read at src/core/internal/bytealg/bytealg.cs:110: LastIndexRabinKarp's `s[last..].ToGoString() == sep.ToGoString()` comparison, two @string copies where Go allocates none -- the reading of 2.",
      "want": "0 allocations per run",
      "reading": "2 per run -- 200 go2cs-runtime object allocations (19,200 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 94; the host's unit note is COUNT, a LOWER BOUND; the test printed `expected no allocations, got 2.000000`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 1 (dated 2026-09-23). the string-conversion family's comparison arm, whose mechanism already ships: the converter's string([]byte) elision emits golib's non-allocating `sstring` view for a provably read-only, non-escaping conversion (docs/ConversionStrategies-Reference.md, *Strings (`@string` and `sstring`)*; bytes.Equal's own body is `((sstring)a) == ((sstring)b)`, src/core/bytes/bytes.cs:25). The increment is that elision reaching a TYPE-PARAMETER operand: in the generic IndexRabinKarp[T string | []byte] the same Go comparison renders through ByteSeqExtensions.ToGoString and copies both sides. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
    },
    {
      "name": "TestNewBufferShallow",
      "class": "deferred",
      "signature": "allocations occurred while shallow copying",
      "reason": "want-zero AllocsPerRun assert: the addressed Buffer copy heap-boxes per run in the managed model (ref var through the heap) where Go's escape analysis keeps the shallow copy on the stack. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the addressed Buffer copy heap-boxes per run, the ж-box-per-address-take family.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 1,000 go2cs-runtime object allocations (104,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 95; the host's unit note is COUNT, a LOWER BOUND; the test printed `allocations occurred while shallow copying`",
      "plan": "the zh-box reduction arc, whose mechanism family is storage Go keeps in the frame that golib places on the heap -- a ж-box minted per address-take, and the heap backing of a non-escaping working buffer (the ruled crypto/rand precedent, ledger 2026-09-22 04:57): docs/phase4/DESIGN-zh-box-reduction.md (the ref-lowering arc) and docs/phase4/DESIGN-zh-box-three-capabilities.md (the increments C0, then I1/I3/I4/I5). This entry's site: the address-taken Buffer copy in the measured closure. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
    },
    {
      "name": "TestWriteAppend",
      "class": "deferred",
      "signature": "allocations occurred while appending",
      "reason": "want-zero AllocsPerRun assert: the converted AvailableBuffer/AppendInt/Write loop allocates in the managed slice model where Go's sufficiently-sized buffer appends allocation-free. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O1): alloc-profile -> deferred. Read at strconv/itoa.cs:89: the counted object per call is formatBits' local 65-byte array, not a slice-model site as this reason supposes.",
      "want": "0 allocations per run",
      "reading": "900 per run -- 90,000 go2cs-runtime object allocations (13,680,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 96 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `allocations occurred while appending`",
      "plan": "docs/phase4/DESIGN-nonescaping-locals.md (REC-B), local face: strconv formatBits' `array<byte> a = new(65)` (strconv/itoa.cs:89), one per AppendInt of a value of 100 or more -- 900 of the 1,000 per run. About 56 B per call of UNCOUNTED residue (an inference from 152 B per counted object) is attributed before a 0-byte retirement. A want-0 entry retires only at ZERO BYTES (testing.cs:743)."
    }
  ]
}
