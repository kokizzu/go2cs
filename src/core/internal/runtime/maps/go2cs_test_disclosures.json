{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestTableGroupCount/makemap/n=-1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=-1073741824/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=0/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=12/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=14/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=15/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=24/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=29/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=8/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap/n=9/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=-1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=-1073741824/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=0/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=12/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=14/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=15/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=24/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=29/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=8/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/makemap64/n=9/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=-1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=-1073741824/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=0/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=12/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=14/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=15/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=24/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=29/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=8/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/mapliteral/n=9/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=-1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=-1073741824/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=0/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=1/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=12/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=14/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=15/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=24/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=29/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=8/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    },
    {
      "name": "TestTableGroupCount/nohint/n=9/escape",
      "class": "runtime-capability",
      "signature": "dereference of a managed pointer with no address",
      "reason": "runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(&m)), emitted Reinterpret<map<nint, nint>, ж<Map>>() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map<K,V> is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22."
    }
  ]
}
