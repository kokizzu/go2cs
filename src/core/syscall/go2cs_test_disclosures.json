{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestAmbientCaps",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Credential is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Credential. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Credential -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestAmbientCapsUserns",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Credential is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Credential. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Credential -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestCloneNEWUSERAndRemap/setgroups=false",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Cloneflags is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Cloneflags. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Cloneflags -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestCloneNEWUSERAndRemap/setgroups=true",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Cloneflags is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Cloneflags. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Cloneflags -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestCloneTimeNamespace",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Cloneflags is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Cloneflags. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Cloneflags -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestDeathSignalSetuid",
      "class": "platform-skip",
      "signature": "parent could not run child program as selected user",
      "reason": "Same posix_spawn seam as the twelve entries beside it -- see any of them for the full statement of the property and its os/exec host-limit precedent. This test's skip text is the only one of the thirteen that does not carry the seam message: it reports the CONSEQUENCE (\"parent could not run child program as selected user\") rather than the refused field, because it runs its child through a helper that reports the failure in its own words. Pinned to that stable clause; the raw output continues with \"\\nstderr:\\n0x7f...\", an address that would make a verbatim signature match once and never again. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestEmptyCredGroupsDisableSetgroups",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Credential is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Credential. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Credential -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestGroupCleanup",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Credential is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Credential. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Credential -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestGroupCleanupUserNamespace",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Credential is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Credential. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Credential -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestPidFDWithUserNS",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Cloneflags is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Cloneflags. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Cloneflags -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestUnshare",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Unshareflags is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Unshareflags. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Unshareflags -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestUnshareMountNameSpace",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Unshareflags is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Unshareflags. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Unshareflags -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    },
    {
      "name": "TestUnshareMountNameSpaceChroot",
      "class": "platform-skip",
      "signature": "posix_spawn seam: SysProcAttr.Chroot is not supported",
      "reason": "The converted deployment spawns children through posix_spawn(3), and its seam NAMES each SysProcAttr field it cannot express and refuses the request rather than silently ignoring it -- the same structural property os/exec's committed TestCredentialNoSetGroups entry discloses under host-limit (coordinator ruling 2026-08-29), reached here from a second package. That entry names Credential alone because Credential is all its suite reaches; this seam declares refusal for eleven fields and syscall's suite reaches Chroot. The child never starts, and this test's OWN error handling converts a failed spawn into a skip -- which is why the same ruled property carries host-limit there (Go=pass/C#=fail) and platform-skip here (Go=pass/C#=skip): the class describes the VERDICT SHAPE, not the root. Go reports pass because a real toolchain build spawns via fork+exec and can apply the field; the converted side reports skip by executing the branch Go's own source wrote for an environment that cannot. Self-retiring in this class's required way: the entry stops firing the moment the spawn seam gains Chroot -- no corpus change and no manifest edit. runtime-capability's refusal does not apply: nothing here is unimplemented-but-fixable, and the managed fork-window dance that could express these fields was DELIBERATELY retired for file-descriptor-hygiene soundness, which is a ruled correctness decision rather than an omission. SIGNATURE NOTE: pinned to the seam message ALONE. The raw skip output also carries a pointer address and a random temp path (\"skipping: 0x7f...: fork/exec /tmp/gotest.../...\"), so a signature taken from it verbatim would match exactly once, ever. Captured from the converted host's own -json stream. Coordinator ruling, 2026-09-02"
    }
  ]
}
