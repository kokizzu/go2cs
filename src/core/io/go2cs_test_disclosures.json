{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestPipeAllocations",
      "class": "deferred",
      "signature": "too many allocations for io.Pipe() call: ",
      "reason": "count-bound AllocsPerRun assert (want <= 4): the byte-derived shim reports allocated bytes, so Pipe() - measured 1184 bytes per call, for the two heap-boxed ends plus the pipe struct's three channels and its Mutex/Once/onceError members - can never satisfy a four-MALLOC bound whatever its allocation behavior. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O4): alloc-count-semantics -> deferred. The byte-derived-shim premise is retired: the reading is COUNT. Its plan is REC-G, not zh-box.",
      "want": "4 allocations per run at most",
      "reading": "14 per run -- 140 go2cs-runtime object allocations (8,000 bytes) counted over 10 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 120 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `too many allocations for io.Pipe() call: 14.000000`",
      "plan": "docs/phase4/DESIGN-channels.md §6 (REC-G): a single-object channel core (owner R, after its S-arc), predicted 14 -> 5 = Go's own 4 (the PipeWriter box and the three channels, go1.24.13 io/pipe.go) + 1 field-ref view (`pw.of(PipeWriter.Ꮡr)`, io/pipe.cs:253), UNMEASURED. No record removes the view today (REC-G states why), so 5 against the want of 4 is a prediction, not a floor. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    }
  ]
}
