{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestAllocationsDecode",
      "class": "deferred",
      "signature": "Decode allocated ",
      "reason": "want-zero AllocsPerRun assert: Decode returns a []rune that the managed runtime heap-allocates where Go's escape analysis stack-allocates the non-escaping result (the test is guarded by testenv.SkipIfOptimizationOff precisely because the zero-alloc outcome depends on the optimizer); a malloc-counting shim would fail identically. TestDecode proves the decoded output is correct — only the allocation profile diverges. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O5): alloc-profile -> deferred. Legs 2-4's unit is unrecorded because the host notes only a test's first nonzero call; each mints Decode's counted make on every run, so they are COUNT by construction.",
      "want": "0 allocations per run",
      "reading": "1 per run -- 10 go2cs-runtime object allocations (2,800 bytes) counted over 10 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 127 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `Decode allocated 1 times | Decode allocated 1 times | Decode allocated 1 times | Decode allocated 1 times`; that is leg 1; legs 2-4 print 1, COUNT by construction (Decode's make is counted on every run); four legs, not three",
      "plan": "docs/phase4/DESIGN-nonescaping-locals.md (REC-B), the inlined-call-site stage: Decode's `make([]rune, 0, 64)` (go1.24.13 utf16.go:119) is returned through decode(s, buf) (unicode/utf16/utf16.cs:124-125), and Go keeps it on the stack only because Decode inlines; predicted 1 -> 0 on each of the four legs (UNMEASURED). A want-0 entry retires only at ZERO BYTES (testing.cs:743). (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    }
  ]
}
