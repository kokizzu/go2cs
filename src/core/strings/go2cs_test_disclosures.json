{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestBuilderAllocs",
      "class": "deferred",
      "signature": "Builder allocs = ",
      "reason": "exact-count AllocsPerRun assert (want 1): the managed shim is deliberately byte-derived (no CLR malloc counter), so a nonzero count assert can never agree — see the AllocsPerRun entry in docs/ConversionStrategies-Reference.md. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-count-semantics -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: the test is Go's own issue-23382 check that copyCheck does not force the Builder to escape, and the converted `var b Builder` is a ж<Builder> (copyCheck stores the address, src/core/strings/builder.cs:26-35) beside the buffer -- the reading of 2. The alloc-count-semantics premise (a byte-derived shim) is retired by the same note. RE-SIGNED 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O4): the attribution above (a ж<Builder> box beside the buffer) is CORRECTED -- the two counted objects are the box and String()'s ElemRefBox; the buffer is uncounted. The zh-box plan is dropped. The strings.Builder box sits at the identity-keyed boundary -- copyCheck stores and compares the Builder's own address (strings/builder.cs:26-40) -- which the zh-box vetoes exclude (X1/X2, DESIGN-zh-box-reduction.md:162-165; XM-6, DESIGN-zh-box-b-prime.md:583-585; DESIGN-zh-box-three-capabilities.md §6), so zh-box is NOT its plan.",
      "want": "1 allocation per run",
      "reading": "2 per run -- 20,000 go2cs-runtime object allocations (1,920,000 bytes) counted over 10,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 97 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `Builder allocs = 2; want 1`",
      "plan": "docs/phase4/DESIGN-string-byte-window.md §6 (REC-D, the mirror arm) for the ElemRefBox String() mints (strings/builder.cs:44 -> unsafe.cs:985) and docs/phase4/DESIGN-allocation-counting.md §9 (REC-F) (iii) for the buffer, which is Go's one allocation and uncounted today (bytealg_impl.cs:24). After REC-D alone the entry reads 1 -- the box, not the buffer -- so a pass there rests on the counter's coverage boundary, and REC-F (iii) takes it back to 2. FLOOR 2, in prose only: the ж<Builder> box plus the buffer, once REC-F (iii) counts it, against want 1. END STATE: when the reading reaches 2 the entry relabels structural on the identity-keyed veto (copyCheck stores and compares the Builder's own address, strings/builder.cs:26-40). (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestBuilderGrow",
      "class": "deferred",
      "signature": " allocs during Write; want ",
      "reason": "exact-count AllocsPerRun asserts (want 1 per growLen>0 leg, want 0 for growLen=0): the byte-derived shim can never agree on a nonzero count, and the growLen=0 leg's addressed Builder heap-boxes per run where Go's escape analysis stack-allocates. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O4): alloc-count-semantics -> deferred. The byte-derived-shim premise is retired: every failing leg reads COUNT. The strings.Builder box sits at the identity-keyed boundary -- copyCheck stores and compares the Builder's own address (strings/builder.cs:26-40) -- which the zh-box vetoes exclude (X1/X2, DESIGN-zh-box-reduction.md:162-165; XM-6, DESIGN-zh-box-b-prime.md:583-585; DESIGN-zh-box-three-capabilities.md §6), so zh-box is NOT its plan.",
      "want": "0 allocations on the growLen=0 leg and 1 on each growLen>0 leg",
      "reading": "2 per run -- 200 go2cs-runtime object allocations (20,000 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 98 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `growLen=0: got 2 allocs during Write; want 0 | growLen=100: got 3 allocs during Write; want 1 | growLen=1000: got 3 allocs during Write; want 1 | growLen=10000: got 3 allocs during Write; want 1 | growLen=100000: got 3 allocs during Write; want 1`; that is the growLen=0 leg; legs 2-5 print 3, COUNT by construction (the heap box at strings/builder_test.cs:140 is counted on every run)",
      "plan": "docs/phase4/DESIGN-allocation-counting.md §9 (REC-F) (iii) for the buffer and its size class; docs/phase4/DESIGN-string-byte-window.md §6 (REC-D, the mirror arm) for String()'s ElemRefBox; docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm) stage 5 for the comparison operand `(@string)p` at strings/builder_test.cs:146. FLOORS, in prose only (a floor must share the counter's unit, and the buffer is uncounted until REC-F (iii)): the growLen=0 leg's floor is 1 -- the ж<Builder> box (strings/builder_test.cs:140) -- against want 0; each growLen>0 leg's is 2 -- the box plus the buffer, once counted -- against want 1. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    },
    {
      "name": "TestBuilderGrowSizeclasses",
      "class": "deferred",
      "signature": "unexpected amount of allocations: ",
      "reason": "count-bound AllocsPerRun assert (want <= 1): the byte-derived shim reports allocated bytes, so the managed Builder path can never satisfy the count bound. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O4): alloc-count-semantics -> deferred. The byte-derived-shim premise is retired: the reading is COUNT. 3 per run = the ж<Builder> box + the ElemRefBox + the append regrow; the byte[18] MakeNoZero allocates is uncounted. The strings.Builder box sits at the identity-keyed boundary -- copyCheck stores and compares the Builder's own address (strings/builder.cs:26-40) -- which the zh-box vetoes exclude (X1/X2, DESIGN-zh-box-reduction.md:162-165; XM-6, DESIGN-zh-box-b-prime.md:583-585; DESIGN-zh-box-three-capabilities.md §6), so zh-box is NOT its plan.",
      "want": "1 allocation per run at most",
      "reading": "3 per run -- 300 go2cs-runtime object allocations (27,200 bytes) counted over 100 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 99 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `unexpected amount of allocations: 3, want: 1`",
      "plan": "docs/phase4/DESIGN-allocation-counting.md §9 (REC-F) (iii), MakeNoZero's size class (Go's Grow(18) rounds to 24, so the 19-byte write fits), and docs/phase4/DESIGN-string-byte-window.md §6 (REC-D, the mirror arm) for the ElemRefBox String() mints (strings/builder.cs:44 -> unsafe.cs:985). The box's floor of 2 (box plus the buffer, once counted) is stated here in prose only: a floor must share the counter's unit, and the buffer is uncounted today (bytealg_impl.cs:24)."
    },
    {
      "name": "TestIndexRune",
      "class": "deferred",
      "signature": "expected no allocations, got ",
      "reason": "want-zero AllocsPerRun assert: IndexRune's Index(s, string(r)) materializes a byte[] in the managed model where Go uses a 4-byte stack buffer (runtime.intstring); the index-semantics legs all pass. RELABEL 2026-09-23 (C1, from the i7 reading run at bb54ff0920, Release with tiering off): alloc-profile -> deferred. The run's own unit note is COUNT, so golib's counter SAW these objects -- the COUNT arm of the 2026-09-05 ladder -- and the site family is one a named plan removes: string(r) for a lookup, which Go builds in runtime.intstring's 4-byte stack buffer.",
      "want": "0 allocations per run",
      "reading": "2 per run -- 2,000 go2cs-runtime object allocations (64,000 bytes) counted over 1,000 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off -- the i7 reading run, claude/coord-h10-readings ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv line 100; the host's unit note is COUNT, a LOWER BOUND; the test printed `expected no allocations, got 2.000000`",
      "plan": "RECORD: docs/phase4/DESIGN-string-byte-window.md §7 (the transient-conversion arm), stage 4 (dated 2026-09-23). the string-conversion family -- an intermediate buffer Go never allocates for a conversion whose result is not retained: golib's builtin.tmpstring (the zero-copy transient @string the converter already emits for a map-READ key, docs/ConversionStrategies-Reference.md `m[string(b)]`) extended to a string(b) that the callee or comparison does not retain, with docs/phase4/DESIGN-string-byte-window.md as the converter-side recognition record. This entry's site is the family's rune arm: a transient one-rune @string over a stack 4-byte encoding for a string(r) the callee does not retain, the managed counterpart of runtime.intstring's buffer. Retires when a run reads 0 per run with the counter still charging the path; a reading at the want with the counter charging NOTHING is the byte arm (testing.AllocsPerRun's fallback) and would be a false retire."
    }
  ]
}
