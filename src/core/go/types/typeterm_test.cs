// Code generated by "go test -run=Generate -write=all"; DO NOT EDIT.
// Source: ../../cmd/compile/internal/types2/typeterm_test.go
// Copyright 2021 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.
[assembly: global::go.GoPositionMap("go/types/typeterm_test.go", "typeterm_test.cs", "ABIgggANGoKCgILagoKClKaCgoKUpoIADyCCgoKCgIK2goCC2qIAH0KCgoKCgoCC2oIAESSCgoKCgIK2goCC2oIACRSCgoKCgILaggASJoKCgoKAgtqCAAsYgoKCgoCCtoKAgg==")]

namespace go.go;

using strings = strings_package;
using testing = testing_package;
using static global::go.go.types_package;
using token = global::go.go.token_package;

partial class types_internal_test_package {

internal static global::go.go.types_package.ΔType myInt;
internal static void initᴛmyInt() { myInt = ((Func<global::go.go.types_package.ΔType>)(() => {
    var tname = NewTypeName(nopos, nil, "myInt"u8, default!);
    return new global::go.go.types_package.NamedжΔType(NewNamed(tname, new global::go.go.types_package.BasicжΔType(Typ[Int]), default!));
}))(); }

internal static map<@string, ж<global::go.go.types_package.term>> testTerms;
internal static void initᴛtestTerms() { testTerms = new map<@string, ж<global::go.go.types_package.term>>{
    ["∅"u8] = default!,
    ["𝓤"u8] = Ꮡ(new global::go.go.types_package.term()),
    ["int"u8] = Ꮡ(new global::go.go.types_package.term(false, new global::go.go.types_package.BasicжΔType(Typ[Int]))),
    ["~int"u8] = Ꮡ(new global::go.go.types_package.term(true, new global::go.go.types_package.BasicжΔType(Typ[Int]))),
    ["string"u8] = Ꮡ(new global::go.go.types_package.term(false, new global::go.go.types_package.BasicжΔType(Typ[ΔString]))),
    ["~string"u8] = Ꮡ(new global::go.go.types_package.term(true, new global::go.go.types_package.BasicжΔType(Typ[ΔString]))),
    ["myInt"u8] = Ꮡ(new global::go.go.types_package.term(false, myInt))
}; }

public static void TestTermString(ж<testing.T> Ꮡt) {
    foreach (var (want, x) in testTerms) {
        {
            @string got = x.String(); if (got != want) {
                Ꮡt.Errorf("%v.String() == %v; want %v"u8, x.OrTypedNil(), got, want);
            }
        }
    }
}

internal static slice<@string> split(@string s, nint n) {
    var r = strings.Split(s, " "u8);
    if (len(r) != n) {
        throw panic("invalid test case: " + s);
    }
    return r;
}

internal static ж<global::go.go.types_package.term> testTerm(@string name) {
    var (r, ok) = testTerms[name, ꟷ];
    if (!ok) {
        throw panic("invalid test argument: " + name);
    }
    return r;
}

public static void TestTermEqual(ж<testing.T> Ꮡt) {
    foreach (var (_, test) in new @string[]{
        "∅ ∅ T"u8,
        "𝓤 𝓤 T"u8,
        "int int T"u8,
        "~int ~int T"u8,
        "myInt myInt T"u8,
        "∅ 𝓤 F"u8,
        "∅ int F"u8,
        "∅ ~int F"u8,
        "𝓤 int F"u8,
        "𝓤 ~int F"u8,
        "𝓤 myInt F"u8,
        "int ~int F"u8,
        "int myInt F"u8,
        "~int myInt F"u8
    }.slice()) {
        var args = split(test, 3);
        var x = testTerm(args[0]);
        var y = testTerm(args[1]);
        var want = args[2] == "T";
        {
            var got = x.equal(y); if (got != want) {
                Ꮡt.Errorf("%v.equal(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got, want);
            }
        }
        // equal is symmetric
        (x, y) = (y, x);
        {
            var got = x.equal(y); if (got != want) {
                Ꮡt.Errorf("%v.equal(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got, want);
            }
        }
    }
}

public static void TestTermUnion(ж<testing.T> Ꮡt) {
    ref var t = ref Ꮡt.DerefOrNull();

    foreach (var (_, test) in new @string[]{
        "∅ ∅ ∅ ∅"u8,
        "∅ 𝓤 𝓤 ∅"u8,
        "∅ int int ∅"u8,
        "∅ ~int ~int ∅"u8,
        "∅ myInt myInt ∅"u8,
        "𝓤 𝓤 𝓤 ∅"u8,
        "𝓤 int 𝓤 ∅"u8,
        "𝓤 ~int 𝓤 ∅"u8,
        "𝓤 myInt 𝓤 ∅"u8,
        "int int int ∅"u8,
        "int ~int ~int ∅"u8,
        "int string int string"u8,
        "int ~string int ~string"u8,
        "int myInt int myInt"u8,
        "~int ~string ~int ~string"u8,
        "~int myInt ~int ∅"u8, // union is symmetric, but the result order isn't - repeat symmetric cases explicitly

        "𝓤 ∅ 𝓤 ∅"u8,
        "int ∅ int ∅"u8,
        "~int ∅ ~int ∅"u8,
        "myInt ∅ myInt ∅"u8,
        "int 𝓤 𝓤 ∅"u8,
        "~int 𝓤 𝓤 ∅"u8,
        "myInt 𝓤 𝓤 ∅"u8,
        "~int int ~int ∅"u8,
        "string int string int"u8,
        "~string int ~string int"u8,
        "myInt int myInt int"u8,
        "~string ~int ~string ~int"u8,
        "myInt ~int ~int ∅"u8
    }.slice()) {
        var args = split(test, 4);
        var x = testTerm(args[0]);
        var y = testTerm(args[1]);
        var want1 = testTerm(args[2]);
        var want2 = testTerm(args[3]);
        {
            var (got1, got2) = x.union(y); if (!got1.equal(want1) || !got2.equal(want2)) {
                Ꮡt.Errorf("%v.union(%v) = %v, %v; want %v, %v"u8, x.OrTypedNil(), y.OrTypedNil(), got1.OrTypedNil(), got2.OrTypedNil(), want1.OrTypedNil(), want2.OrTypedNil());
            }
        }
    }
}

public static void TestTermIntersection(ж<testing.T> Ꮡt) {
    foreach (var (_, test) in new @string[]{
        "∅ ∅ ∅"u8,
        "∅ 𝓤 ∅"u8,
        "∅ int ∅"u8,
        "∅ ~int ∅"u8,
        "∅ myInt ∅"u8,
        "𝓤 𝓤 𝓤"u8,
        "𝓤 int int"u8,
        "𝓤 ~int ~int"u8,
        "𝓤 myInt myInt"u8,
        "int int int"u8,
        "int ~int int"u8,
        "int string ∅"u8,
        "int ~string ∅"u8,
        "int string ∅"u8,
        "~int ~string ∅"u8,
        "~int myInt myInt"u8
    }.slice()) {
        var args = split(test, 3);
        var x = testTerm(args[0]);
        var y = testTerm(args[1]);
        var want = testTerm(args[2]);
        {
            var got = x.intersect(y); if (!got.equal(want)) {
                Ꮡt.Errorf("%v.intersect(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got.OrTypedNil(), want.OrTypedNil());
            }
        }
        // intersect is symmetric
        (x, y) = (y, x);
        {
            var got = x.intersect(y); if (!got.equal(want)) {
                Ꮡt.Errorf("%v.intersect(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got.OrTypedNil(), want.OrTypedNil());
            }
        }
    }
}

public static void TestTermIncludes(ж<testing.T> Ꮡt) {
    foreach (var (_, test) in new @string[]{
        "∅ int F"u8,
        "𝓤 int T"u8,
        "int int T"u8,
        "~int int T"u8,
        "~int myInt T"u8,
        "string int F"u8,
        "~string int F"u8,
        "myInt int F"u8
    }.slice()) {
        var args = split(test, 3);
        var x = testTerm(args[0]);
        var y = testTerm(args[1]).Value.typ;
        var want = args[2] == "T";
        {
            var got = x.includes(y); if (got != want) {
                Ꮡt.Errorf("%v.includes(%v) = %v; want %v"u8, x.OrTypedNil(), y, got, want);
            }
        }
    }
}

public static void TestTermSubsetOf(ж<testing.T> Ꮡt) {
    foreach (var (_, test) in new @string[]{
        "∅ ∅ T"u8,
        "𝓤 𝓤 T"u8,
        "int int T"u8,
        "~int ~int T"u8,
        "myInt myInt T"u8,
        "∅ 𝓤 T"u8,
        "∅ int T"u8,
        "∅ ~int T"u8,
        "∅ myInt T"u8,
        "𝓤 int F"u8,
        "𝓤 ~int F"u8,
        "𝓤 myInt F"u8,
        "int ~int T"u8,
        "int myInt F"u8,
        "~int myInt F"u8,
        "myInt int F"u8,
        "myInt ~int T"u8
    }.slice()) {
        var args = split(test, 3);
        var x = testTerm(args[0]);
        var y = testTerm(args[1]);
        var want = args[2] == "T";
        {
            var got = x.subsetOf(y); if (got != want) {
                Ꮡt.Errorf("%v.subsetOf(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got, want);
            }
        }
    }
}

public static void TestTermDisjoint(ж<testing.T> Ꮡt) {
    foreach (var (_, test) in new @string[]{
        "int int F"u8,
        "~int ~int F"u8,
        "int ~int F"u8,
        "int string T"u8,
        "int ~string T"u8,
        "int myInt T"u8,
        "~int ~string T"u8,
        "~int myInt F"u8,
        "string myInt T"u8,
        "~string myInt T"u8
    }.slice()) {
        var args = split(test, 3);
        var x = testTerm(args[0]);
        var y = testTerm(args[1]);
        var want = args[2] == "T";
        {
            var got = x.disjoint(y); if (got != want) {
                Ꮡt.Errorf("%v.disjoint(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got, want);
            }
        }
        // disjoint is symmetric
        (x, y) = (y, x);
        {
            var got = x.disjoint(y); if (got != want) {
                Ꮡt.Errorf("%v.disjoint(%v) = %v; want %v"u8, x.OrTypedNil(), y.OrTypedNil(), got, want);
            }
        }
    }
}

} // end types_internal_test_package
