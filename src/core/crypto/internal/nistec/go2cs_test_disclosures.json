{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestAllocations/P224",
      "class": "alloc-profile",
      "signature": "expected zero allocations, got ",
      "reason": "want-zero AllocsPerRun assert over two full scalar multiplications plus point encode/decode round-trips: Go's escape analysis proves every point and fiat field-element temporary in the ladder non-escaping (even NewP224Point() stays on the stack), while the managed model heap-boxes each one — 8,484 golib objects per run at the B2 kind-split emission, so zero is structurally unreachable"
    },
    {
      "name": "TestAllocations/P256",
      "class": "alloc-profile",
      "signature": "expected zero allocations, got ",
      "reason": "want-zero AllocsPerRun assert over two full scalar multiplications plus point encode/decode round-trips: Go's escape analysis proves every point and fiat field-element temporary in the ladder non-escaping (even NewP256Point() stays on the stack), while the managed model heap-boxes each one — 8,528 golib objects per run at the B2 kind-split emission, so zero is structurally unreachable"
    },
    {
      "name": "TestAllocations/P384",
      "class": "alloc-profile",
      "signature": "expected zero allocations, got ",
      "reason": "want-zero AllocsPerRun assert over two full scalar multiplications plus point encode/decode round-trips: Go's escape analysis proves every point and fiat field-element temporary in the ladder non-escaping (even NewP384Point() stays on the stack), while the managed model heap-boxes each one — 12,572 golib objects per run at the B2 kind-split emission, so zero is structurally unreachable"
    },
    {
      "name": "TestAllocations/P521",
      "class": "alloc-profile",
      "signature": "expected zero allocations, got ",
      "reason": "want-zero AllocsPerRun assert over two full scalar multiplications plus point encode/decode round-trips: Go's escape analysis proves every point and fiat field-element temporary in the ladder non-escaping (even NewP521Point() stays on the stack), while the managed model heap-boxes each one — 17,090 golib objects per run at the B2 kind-split emission, so zero is structurally unreachable"
    }
  ]
}
