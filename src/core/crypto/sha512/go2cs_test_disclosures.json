{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestAllocations",
      "class": "deferred",
      "signature": "allocs = ",
      "reason": "want-zero AllocsPerRun assert: Sum copies the digest (`d0 := *d`) so the caller can keep writing, and that copy must deep-copy the digest's [8]uint64 state and [128]byte block — golib's array<T> is a struct over a heap T[], so the copy is two managed allocations where Go's is stack-resident; Write/Sum also allocate through the hash.Hash interface surface. RELABEL 2026-09-23 (C1, as ruled at ledger 2026-09-23 03:37 O1): alloc-profile -> deferred. 106 per run = 88 + 16 + 2. The measured closure mints a source-generated hash.Hash shell for every New* call on every run (sha512_test.cs:947-965; sha512.cs:43/51/59/70): 4 per run, UNCOUNTED (AllocationCounter.cs:50-53), ruled structural on 2026-08-25.",
      "want": "0 allocations per run",
      "reading": "106 per run -- 1,060 go2cs-runtime object allocations (166,080 bytes) counted over 10 runs at bb54ff0920 (the batch-8b stamp), Release with tiering off, readings-go1.24.13.tsv line 119 (claude/coord-h10-readings ac9f8251ee); unit COUNT, a LOWER BOUND; the test printed `allocs = 106, want 0`",
      "plan": "docs/phase4/DESIGN-array-value-storage.md (REC-A) for 60 -- 32 field arrays (16 of `New*`'s `@new<Digest>`, 16 zero fields of `Sum`'s `d0`, which REC-A's @new-then-overwrite increment removes at crypto/internal/fips140/sha512/sha512.cs:272-273) and the copy face: ΔClone 16, `.Clone()` 8 (:313), `sum.Clone()` 4 (crypto/sha512/sha512.cs:82 and siblings); docs/phase4/DESIGN-nonescaping-locals.md (REC-B) for the 28 locals -- `tmp` 8 (:283), `digest` 8 (:302), `w` 8 (sha512block.cs:98), `sum` 4 (crypto/sha512/sha512.cs:80 and siblings), per COORD's ruling 2 -- the 16 class-3b (value carriers) and the 2 slices (constant-capacity make). 106 = 60 + 28 + 16 + 2. END STATE, O1's explicit exception to X(4) (COORD's ruling 5, ledger 3942e083ad): when the counted families reach 0 the entry reads BYTES on the 4 uncounted shells per run and re-labels structural on the shell proof (BOARD:19149-19153) unless the post-hop re-examination re-rules the shell against its named successor (DESIGN-iface-shell-caching.md:61). No floor. (Plan fixed up 2026-09-23 per COORD's ACCEPT-WITH-FIXES, ledger 3942e083ad.)"
    }
  ]
}
