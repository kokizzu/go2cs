# `crypto/md5` — validation proof

Go's own `crypto/md5` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `crypto/md5` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-02 · converter `8dc18ad33`*

**11 matched · 1 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/crypto/md5`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/crypto/md5).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestAllocations` | pass | fail ([disclosed](#disclosed-divergences)) |
| `TestBlockGeneric` | pass | pass |
| `TestGolden` | pass | pass |
| `TestGoldenMarshal` | pass | pass |
| `TestLarge` | pass | pass |
| `TestLargeHashes` | pass | pass |
| `TestMD5Hash` | pass | pass |
| `TestMD5Hash/OutOfBoundsRead` | pass | pass |
| `TestMD5Hash/ResetState` | pass | pass |
| `TestMD5Hash/StatefulWrite` | pass | pass |
| `TestMD5Hash/SumAppend` | pass | pass |
| `TestMD5Hash/WriteWithoutError` | pass | pass |

## Disclosed divergences

A disclosed divergence is a specific Go assertion the managed CLR *provably cannot* satisfy — not
a skipped test and not a tolerance. Each one is pinned by exact failure signature in the package's
hand-owned [`go2cs_test_disclosures.json`](https://github.com/ritchiecarroll/go2cs/blob/master/src/core/crypto/md5/go2cs_test_disclosures.json);
a disclosed test that fails any *other* way is still a hard mismatch.

| Test | Class | Pinned reason |
|:--|:--|:--|
| `TestAllocations` | `alloc-profile` | want-zero AllocsPerRun assert: Sum copies the digest (`d0 := *d`) so the caller can keep writing, and that copy must deep-copy the digest's [4]uint32 state and [64]byte block — golib's array<T> is a struct over a heap T[], so the copy is two managed allocations where Go's is stack-resident; Write/Sum also allocate through the hash.Hash interface surface |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a testing capability the host does not yet provide.

- BenchmarkHash128 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash1K (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash1KUnaligned (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash1M (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash256 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash512 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash64 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash8Bytes (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash8BytesUnaligned (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash8K (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash8KUnaligned (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkHash8M (benchmark): benchmark execution is deferred to Phase 4D
- ExampleNew (example): example execution is deferred to Phase 4D
- ExampleNew_file (example): example execution is deferred to Phase 4D
- ExampleSum (example): example execution is deferred to Phase 4D
