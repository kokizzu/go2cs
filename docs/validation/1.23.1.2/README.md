# Validation proofs — 1.23.1.2 (interim snapshot)

**Interim snapshot, created by hand on 2026-08-02** as a copy of [`../current/`](../current), so the
version-pinned proof links carried by the published package READMEs resolve from the day the badges
landed. Every other versioned directory here is written by `src/push-nuget.ps1` at publication and is
write-once; this one predates that step, and the first release-created snapshot supersedes it.

Its 71 pages are the living proofs as of `1.23.1.2` and are frozen from now on: the pages under
`current/` keep moving as packages re-validate, this directory does not.
