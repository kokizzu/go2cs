# `compress/zlib` — validation proof

Go's own `compress/zlib` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `compress/zlib` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-02 · converter `8dc18ad33`*

**6 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/compress/zlib`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/compress/zlib).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestDecompressor` | pass | pass |
| `TestWriter` | pass | pass |
| `TestWriterBig` | pass | pass |
| `TestWriterDict` | pass | pass |
| `TestWriterDictIsUsed` | pass | pass |
| `TestWriterReset` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a testing capability the host does not yet provide.

- ExampleNewReader (example): example execution is deferred to Phase 4D
- ExampleNewWriter (example): example execution is deferred to Phase 4D
