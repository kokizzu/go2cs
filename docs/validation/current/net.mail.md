# `net/mail` — validation proof

Go's own `net/mail` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `net/mail` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-25 · converter `a338d351d`*

**11 matched · 0 disclosed** — Go 1.23.12, `windows/amd64`, converted package
[`src/core/net/mail`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/net/mail).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestAddressFormattingAndParsing` | pass | pass |
| `TestAddressParser` | pass | pass |
| `TestAddressParsing` | pass | pass |
| `TestAddressParsingAndFormatting` | pass | pass |
| `TestAddressParsingError` | pass | pass |
| `TestAddressParsingError/CustomWordDecoder` | pass | pass |
| `TestAddressString` | pass | pass |
| `TestDateParsing` | pass | pass |
| `TestDateParsingCFWS` | pass | pass |
| `TestEmptyAddress` | pass | pass |
| `TestParsing` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- ExampleParseAddress (example): example execution is deferred to Phase 4D
- ExampleParseAddressList (example): example execution is deferred to Phase 4D
- ExampleReadMessage (example): example execution is deferred to Phase 4D
