# `internal/profile` — validation proof

Go's own `internal/profile` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `internal/profile` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-09 · converter `4990c0043`*

**1 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/internal/profile`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/internal/profile).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestPackedEncoding` | pass | pass |
