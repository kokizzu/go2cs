# `compress/bzip2` — validation proof

Go's own `compress/bzip2` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `compress/bzip2` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-02 · converter `8dc18ad33`*

**4 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/compress/bzip2`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/compress/bzip2).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestBitReader` | pass | pass |
| `TestMTF` | pass | pass |
| `TestReader` | pass | pass |
| `TestZeroRead` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a testing capability the host does not yet provide.

- BenchmarkDecodeDigits (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkDecodeNewton (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkDecodeRand (benchmark): benchmark execution is deferred to Phase 4D
