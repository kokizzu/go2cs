# `io/ioutil` — validation proof

Go's own `io/ioutil` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `io/ioutil` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-13 · converter `9f8036b36`*

**28 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/io/ioutil`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/io/ioutil).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestReadDir` | pass | pass |
| `TestReadFile` | pass | pass |
| `TestReadOnlyWriteFile` | pass | pass |
| `TestTempDir` | pass | pass |
| `TestTempDir/*xyz` | pass | pass |
| `TestTempDir/ioutil_test` | pass | pass |
| `TestTempDir/ioutil_test*` | pass | pass |
| `TestTempDir/ioutil_test*xyz` | pass | pass |
| `TestTempDir_BadDir` | pass | pass |
| `TestTempDir_BadPattern` | pass | pass |
| `TestTempDir_BadPattern/\ioutil_test\*foo` | pass | pass |
| `TestTempDir_BadPattern/ioutil*test` | pass | pass |
| `TestTempDir_BadPattern/ioutil_test*\foo` | pass | pass |
| `TestTempDir_BadPattern/ioutil_test*foo` | pass | pass |
| `TestTempDir_BadPattern/ioutil_test*foo\` | pass | pass |
| `TestTempDir_BadPattern/ioutil_test\*foo` | pass | pass |
| `TestTempDir_BadPattern/ioutil_test\foo` | pass | pass |
| `TestTempFile` | pass | pass |
| `TestTempFile_BadPattern` | pass | pass |
| `TestTempFile_BadPattern/\ioutil_test\*foo` | pass | pass |
| `TestTempFile_BadPattern/ioutil*test` | pass | pass |
| `TestTempFile_BadPattern/ioutil_test*\foo` | pass | pass |
| `TestTempFile_BadPattern/ioutil_test*foo` | pass | pass |
| `TestTempFile_BadPattern/ioutil_test*foo\` | pass | pass |
| `TestTempFile_BadPattern/ioutil_test\*foo` | pass | pass |
| `TestTempFile_BadPattern/ioutil_test\foo` | pass | pass |
| `TestTempFile_pattern` | pass | pass |
| `TestWriteFile` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- ExampleReadAll (example): example execution is deferred to Phase 4D
- ExampleReadDir (example): example execution is deferred to Phase 4D
- ExampleReadFile (example): example execution is deferred to Phase 4D
- ExampleTempDir (example): example execution is deferred to Phase 4D
- ExampleTempDir_suffix (example): example execution is deferred to Phase 4D
- ExampleTempFile (example): example execution is deferred to Phase 4D
- ExampleTempFile_suffix (example): example execution is deferred to Phase 4D
- ExampleWriteFile (example): example execution is deferred to Phase 4D
