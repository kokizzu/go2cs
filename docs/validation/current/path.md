# `path` — validation proof

Go's own `path` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `path` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-02 · converter `8dc18ad33`*

**9 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/path`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/path).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestBase` | pass | pass |
| `TestClean` | pass | pass |
| `TestCleanMallocs` | pass | pass |
| `TestDir` | pass | pass |
| `TestExt` | pass | pass |
| `TestIsAbs` | pass | pass |
| `TestJoin` | pass | pass |
| `TestMatch` | pass | pass |
| `TestSplit` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a testing capability the host does not yet provide.

- ExampleBase (example): example execution is deferred to Phase 4D
- ExampleClean (example): example execution is deferred to Phase 4D
- ExampleDir (example): example execution is deferred to Phase 4D
- ExampleExt (example): example execution is deferred to Phase 4D
- ExampleIsAbs (example): example execution is deferred to Phase 4D
- ExampleJoin (example): example execution is deferred to Phase 4D
- ExampleMatch (example): example execution is deferred to Phase 4D
- ExampleSplit (example): example execution is deferred to Phase 4D
