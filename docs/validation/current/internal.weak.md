# `internal/weak` — validation proof

Go's own `internal/weak` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `internal/weak` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-30 · converter `aba8870fe`*

**4 matched · 0 disclosed** — Go 1.23.12, `windows/amd64`, converted package
[`src/core/internal/weak`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/internal/weak).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestIssue69210` | pass | pass |
| `TestPointer` | pass | pass |
| `TestPointerEquality` | pass | pass |
| `TestPointerFinalizer` | pass | pass |
