# `testing/iotest` — validation proof

Go's own `testing/iotest` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `testing/iotest` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-07 · converter `d6aa09e73`*

**18 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/testing/iotest`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/testing/iotest).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestDataErrReader_emptyReader` | pass | pass |
| `TestDataErrReader_nonEmptyReader` | pass | pass |
| `TestErrReader` | pass | pass |
| `TestErrReader/io.EOF` | pass | pass |
| `TestErrReader/nil_error` | pass | pass |
| `TestErrReader/non-nil_error` | pass | pass |
| `TestHalfReader_emptyReader` | pass | pass |
| `TestHalfReader_nonEmptyReader` | pass | pass |
| `TestOneByteReader_emptyReader` | pass | pass |
| `TestOneByteReader_nonEmptyReader` | pass | pass |
| `TestReadLogger` | pass | pass |
| `TestReadLogger_errorOnRead` | pass | pass |
| `TestStringsReader` | pass | pass |
| `TestTimeOutReader_emptyReader` | pass | pass |
| `TestTimeOutReader_nonEmptyReader` | pass | pass |
| `TestTruncateWriter` | pass | pass |
| `TestWriteLogger` | pass | pass |
| `TestWriteLogger_errorOnWrite` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- ExampleErrReader (example): example execution is deferred to Phase 4D
