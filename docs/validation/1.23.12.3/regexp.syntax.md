# `regexp/syntax` — validation proof

Go's own `regexp/syntax` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `regexp/syntax` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-25 · converter `e2182a59e`*

**12 matched · 0 disclosed** — Go 1.23.12, `windows/amd64`, converted package
[`src/core/regexp/syntax`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/regexp/syntax).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestAppendRangeCollapse` | pass | pass |
| `TestCompile` | pass | pass |
| `TestFoldConstants` | pass | pass |
| `TestParseFoldCase` | pass | pass |
| `TestParseInvalidRegexps` | pass | pass |
| `TestParseLiteral` | pass | pass |
| `TestParseMatchNL` | pass | pass |
| `TestParseNoMatchNL` | pass | pass |
| `TestParseSimple` | pass | pass |
| `TestSimplify` | pass | pass |
| `TestString` | pass | pass |
| `TestToStringEquivalentParse` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkEmptyOpContext (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkIsWordChar (benchmark): benchmark execution is deferred to Phase 4D
