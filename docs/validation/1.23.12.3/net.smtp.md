# `net/smtp` — validation proof

Go's own `net/smtp` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `net/smtp` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-25 · converter `2d53460e0`*

**19 matched · 0 disclosed** — Go 1.23.12, `windows/amd64`, converted package
[`src/core/net/smtp`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/net/smtp).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestAuth` | pass | pass |
| `TestAuthFailed` | pass | pass |
| `TestAuthPlain` | pass | pass |
| `TestBasic` | pass | pass |
| `TestClientAuthTrimSpace` | pass | pass |
| `TestExtensions` | pass | pass |
| `TestExtensions/ehlo` | pass | pass |
| `TestExtensions/ehlo_8bitmime` | pass | pass |
| `TestExtensions/ehlo_8bitmime_smtputf8` | pass | pass |
| `TestExtensions/ehlo_smtputf8` | pass | pass |
| `TestExtensions/helo` | pass | pass |
| `TestHello` | pass | pass |
| `TestNewClient` | pass | pass |
| `TestNewClient2` | pass | pass |
| `TestNewClientWithTLS` | pass | pass |
| `TestSendMail` | pass | pass |
| `TestSendMailWithAuth` | pass | pass |
| `TestTLSClient` | pass | pass |
| `TestTLSConnState` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- Example (example): example execution is deferred to Phase 4D
- ExamplePlainAuth (example): example execution is deferred to Phase 4D
- ExampleSendMail (example): example execution is deferred to Phase 4D
