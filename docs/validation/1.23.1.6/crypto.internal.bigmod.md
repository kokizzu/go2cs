# `crypto/internal/bigmod` — validation proof

Go's own `crypto/internal/bigmod` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `crypto/internal/bigmod` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-07 · converter `d6aa09e73`*

**14 matched · 0 disclosed** — Go 1.23.1, `windows/amd64`, converted package
[`src/core/crypto/internal/bigmod`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/crypto/internal/bigmod).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestExp` | pass | pass |
| `TestExpShort` | pass | pass |
| `TestExpand` | pass | pass |
| `TestMod` | pass | pass |
| `TestModAdd` | pass | pass |
| `TestModAddCommutative` | pass | pass |
| `TestModSub` | pass | pass |
| `TestModSubThenAddIdentity` | pass | pass |
| `TestModulusAndNatSizes` | pass | pass |
| `TestMontgomeryRoundtrip` | pass | pass |
| `TestMulReductions` | pass | pass |
| `TestNewModFromBigZero` | pass | pass |
| `TestSetBytes` | pass | pass |
| `TestShiftIn` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkExp (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkExpBig (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkModAdd (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkModMul (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkModSub (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMontgomeryMul (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMontgomeryRepr (benchmark): benchmark execution is deferred to Phase 4D
