# `go/version` — validation proof

Go's own `go/version` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `go/version` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-08-25 · converter `a338d351d`*

**3 matched · 0 disclosed** — Go 1.23.12, `windows/amd64`, converted package
[`src/core/go/version`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/go/version).

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestCompare` | pass | pass |
| `TestIsValid` | pass | pass |
| `TestLang` | pass | pass |
