# Phase 4 — The Road to 100% (living tracker)

> **LIVING coordinator-maintained surface.** Updated at every banking window; every number here
> is re-derived from [`docs/ValidatedTestPackages.md`](../ValidatedTestPackages.md) (the
> authority) and the mailbox record at update time — never carried forward. If this file and
> the roster disagree, the roster is right and this file is stale. ⚠ That applies to the **row
> tables**, not only to the header: a distance table carried forward while its rows banked elsewhere
> named two already-banked packages as unattempted and nearly sent a lane at them (2026-09-02).
> Re-derive every table from the roster at each touch.
> Scope: the **Windows validation roster** (the 100% goal's primary axis). The per-OS parity
> axis is tracked in its own summary line below.

## Where we stand — updated 2026-09-01 evening (the migration train LANDED at `1da726d58` — row 201 is PUBLIC; the evening successor coordinator seated)

| Measure | Value |
|---|---|
| **On master (the migration train)** | **201 / 209 = 96.2% honest** · 27,734 matching · 154 disclosed · raw 201/216 = 93.1% (os/user banked as row 201, denominator honestly grown) — unchanged while the two bosses close: **reflect 115 → 48 real mismatches** (…PtrBytes → extendSlice → InterfaceData both halves → ChanOf/MapOf/MakeChan/Value.Close leaving the linker-record paths, the crash class HALVED 20→10 with each surviving row naming its next root), **runtime -tests 31 → 1** (traceback part 1 merged — cross-function lift unification via liftAtCallBoundary with the honest not-zero-as-sized accounting; CS1955, Lock, and the silent classes all closed earlier), remaining ONE: **CS8175 — now understood as the method-value RECEIVER-SNAPSHOT family (a value receiver must be copied at method-value EVALUATION; every emitted wrapper read it at CALL), landing as ONE train of two commits**: commit 1 (`e723df85d`, G — the composite-literal/call-argument members, the site owns its own hoisted temp rendered in the enclosing context) is MEASURED by i9 (compile clean, 7-file two-seeded diff = prediction, CNR byte-identical at 688) and G's banked-row sweeps at its tip are 641/641 (bytes/strings/encoding-json); commit 2 (the assignment member — census ZERO production sites, real by repro `a b` vs `b b`) MERGED with it — **the family is ON MASTER at `9a5462091`** (union at the merge result: suite ok 289s · CNR byte-identical 688 · bytes 82 / strings 68 / encoding-json 491 / hpke 19 / crypto-tls 400 host-limited), so **runtime `-tests` = 0 compile errors on master**; the NON-IDENT-receiver shapes are the family's fourth face (i9's census: 73 sites — 42 ident safe, 19 method expressions out of scope, **12 reached-real** across hpke/tls/json/go-types/net-http/runtime, index shape genuinely absent; two mechanisms — the ident gate, and a pre-existing slice-header "snapshot" aliasing the backing array) → **commit 3 RULED: the receiver expression is evaluated EXACTLY ONCE for every kind and shape** (G cuts, i9 measures; its own gated train). **The bill's FIRST run-layer finding — runtime's host dying at MODULE INIT through the stub `getg()` — is CLOSED and ON MASTER at `65b6dd5ba`**: `netpollGenericInit` hand-owned as a measured no-op equivalence (a scope gap in the 2026-08-13 netpoll ruling, not a new wall; `netpollBreak` deliberately still throws so `TestNetpollBreak` stays one loud row; two-emission control 2 files per target, windows and linux), and the host BUILDS, RUNS and EXECUTES tests — 848 entries in the probe, the empties a contiguous tail from **`TestCaller` → `moduledata.textAddr()` (symtab.go:663), the next host-killer, named not chased**. i9 prints the FIRST BILL at this master (`CENSUS-runtime-semantic-bill.md`, partial by construction until each killer falls); Stage A closes when commit 3 lands |
| **reflect remaining distance (measured, not guessed)** | **41 — TRAIN 2 (R, `7fe80c9f5`): 45 → 41 in four increments (Len/Cap pointer-to-array arm +0, IsExported hand-own +0, lifted function-local NAMED types carry their Go name 45 → 43, Swapper 43 → 42, the both-named assignment gate at MapIndex/SetMapIndex 42 → 41); the union re-measure at master `3ac4c27c8` confirmed 42 exactly before the sixth landed; the displacement guard gained a GOROOT `_test.go` witness arm after IsExported false-greened its placeholder-only witness on every clean tree** · previously: **45 — r39d Parts A+B MERGED (`7371b9919`: `Both` folds to `Unstamped` at synthType's one authority, the var-decl and nil→directional stamp sites land; TestAll/TestChanOf/TestChanOfDir green, zero broken; census-as-prediction held to the FILE — 3 corpus lines; nistec cost canary 319s on R-LAPTOP vs its 354s baseline, 269s on the i7 as this host's first quiet figure). ⚠ The 45 is MEASURED AT THE PRE-HOP BASE (`e06c04cc7`): reflect's `-tests` assembly is COMPILE-BROKEN at master since the migration train's `0d6549ae5` (the package-wide lift publish binds a PUBLIC lifted struct's member shape to an INTERNAL prior lift — CS0050/51/52; R bisected it converter-level, parent clean; found by R's own sweep because an unbanked package's `-tests` assembly is in no standing gate) — i9 owns the accessibility-tier fix as top priority; the union re-measure of 45 queues behind it. Part C (the nested direction VECTOR) parked at zero yield behind TestTypes #34. R's +0 Len/Cap arms name the construction-cargo family's THIRD member (array dims through a typed-nil pointer, 2 rows) and R's consolidation hands G's typed-nil root the zero-Value singleton cluster: that root now gates 7+ rows, the largest left. Prior record: 48 at the tail-lane tip, RE-MAPPED from a clean record** — 42 fail / 5 crash / 1 empty. Multi-row roots: **r39d narrowing FOUR rows (the largest addressable root — ruled CARRY, R implements)**, typed-nil FOUR rows (G's sequenced arc), StructOf-embedded 3, shouldPanic 2, Select-Dir 2, MakeFunc-nonfunc 2. Five crashes each diagnosed: StructOfTooLarge ROOTED architectural (zero-instance allocation where Go computes a size; GoSynthField carries the dims — decoupling arc queued), SliceAt unsized, GCBits look-before-disclosing, SelectNop real work, MakeFuncVariadic possibly cheap post-mint — 42 `fail` / 7 `infrastructure-error` / 1 empty; crash class 20→7 across the arc, each survivor a NAMED root (GCBits, MakeFunc-variadic, rselect, unsafeslice, StructOfTooLarge, CallSlice ×2). typelinks fully CLOSED (contract-legal empty, non-vacuous pass stated on the record); next: marshalCallArg←Call, shouldPanic, singletons. Two rows sit on ruled/queued items rather than R's queue: TestChanOf/TestChanOfDir on the r39d narrowing (RULED: carry it — the rule's no-consumer premise died on measurement; cost canary applies), TestMapOf + TestFuncOf + TestMethod on the **typed-nil-func lost type word** (THREE rows now; G's narrow wired-slot arm parked ready at f4065f27b, the call-argument WIDENING is the confirmed converter-side arc — sizing-first, G by boundary ownership after the dupe fix), and TestFuncOf also needs **>16-arity delegate minting** (Reflection.Emit, GoStructSynthesis's own kind — queued to the coordinator sub-agent lane ahead of ElemRefBox) |
| Shipped in 1.23.12.2 (the release snapshot) | 189 / 208 = 90.9% · 26,043 matching · 148 disclosed |
| Landed roll-day + today (every merge union-gated) | R's six reflect commits (115→63) with InterfaceData parked-with-spec · i9's residual rounds 1–3 (31→11: UntypedFloat→defined-type bridge, mixed-wrapper comparison casts, whitebox alias second-renderer, BOTH method-expression gaps + the latent cast-invoke grammar fix, anonymous-INTERFACE dedup + struct production-registry check) · G's A2 steps 1–2 + fmt-shim verb family + init-hook design record · owner-ordered session roll executed, all four successors seated or staged · **security standing order (owner): fleet nicknames only on every pushed surface — both public tips scrubbed (`a7595da67` master, `d72878d6d` mailbox), order banked in CLAUDE.md Conventions** · wave plan amended with G's A2-step-3 sizing census (2,125 hooks / 684 files / 314 pkgs; initᴛᴛtests SURVIVES, three controls; two hand-own blockers ride the relocation commit) · G's registry both-sides guard ruled (dead `reflect.methodName` registration rides it, red captured first) |
| **Rows remaining (implementable)** | **7** (os/user BANKED as row 201 this evening — the first excluded row to rejoin under the anti-laundering clause) — reflect (48: the re-mapped tail above), os (682/685; two implementable blockers, neither disclosable: the NetShareAdd byte-buffer fork [queued, next free full lane] and the WriteStringAlloc machinery-alloc arc [+136 attributed and decomposed to the byte, neither root reverts; ElemRefBox (T[],nint) increment queued — lands the row BELOW pre-regression]), unique (**7/20 MEASURED 2026-09-01**, correcting the prior "16/20" — 16 was the comparison's MISMATCH count transcribed as the match count; honest NON-BANK with two blockers: **(A)** the type-name-erasure converter arc — `global using <Name> = object` erases the Go type name so `reflect.TypeFor[T]().Name()` answers empty, 3 logical tests here, NOT disclosable (named-remedy defect + no shared row to pin a signature to), SYSTEMIC at **167** corpus sites incl. `crypto.PublicKey`/`PrivateKey`, census-first arc DISPATCHED 2026-09-01 evening to a coordinator sub-agent (re-measured census + design record, no cut until ruled); **(B)** the 10 GC rows measured **codegen-liveness** by a frame-residency A/B one-arm-per-process with both controls firing — weak/CWT machinery EXONERATED; banks 20/20-with-10-disclosed the day (A) lands), runtime (-tests at **1** — the lone CS8175, i9+G paired on the capture-coordination fix; at ZERO the SEMANTIC BILL prints and Stage A closes), runtime/pprof, runtime/trace, testing (Option 1 ruled, sequenced) |
| In flight (evening, post-landing) | **G:** the axis list for i9's instrument (kind × shape × base storage × mutation-between), then **commit 3** — evaluate-once receiver, one treatment for every kind and shape, guard positions for field-chain/call/index/pointer-ident — then the typed-nil unpark (owed gates: suite, CNR, 4-phase, slnx, two-seeded byte-identity) and the call-argument widening sizing. **i9:** landing the bill CLASSIFIER as a repo instrument (`src/tools/comparison-classifier`, fixtures as tests), then commit 3's measurement (diff == prediction, CNR, runtime compile re-check), then runtime's **FIRST BILL** at master. **R:** r39d in three separable parts (A: the `Both`/`Unstamped` descriptor-key split normalized at synthType's single authority — a defect in R's own ChanOf; B: the two stamp sites the doc claims but the converter lacks — `var` decl and conversion-of-nil; C: direction as a VECTOR for nested channels, the arrayDims mold) — yield 3, TestTypes stays red on its separate #34; then the unwrap-arm sizing census. **Coordinator sub-agent:** the type-name CARRIER arc (unique's blocker A, corpus-wide reflect fidelity) — gen experiment GREEN (five generators, zero carrier mentions: `src/gen` needs no change); A1 (`GoTypeAlias` `Defined` flag) CANCELLED on measurement (the converter's `usingAliasTargetType` already classifies 4,339 inline / **66 named** / 30 alias / 0 unanswerable — the 30 alias hits being exactly the mis-stamp hazard, already declined); **A2a cutting** (carrier declarations at 16 sites + 4 field marks, no descriptor change); A2b + A3 (param 23 + result 37: one new descriptor-cargo slot + key inclusion, cost canary) sizing as ONE increment. **Coordinator:** three trains landed tonight behind their own union batteries — the migration train (`1da726d58`), the family + census record (`26f3aaa67`), getg (`65b6dd5ba`); sweep dirt classified as the wave's known shapes each time and restored (the corpus now also OWES the family's 12-site emission at Stage B); GitHub branches pruned 123 → 19 heads (102 merged tips, 0 errors); Stage B opens when commit 3 is on master |
| Linux parity axis | 192 of 199 applicable annotated (os/user linux: 12 and plugin linux: 1 behind the per-package cgo-OFF pin -- three production-conditional rows pinned, reflect pinned under the test-plus-missing-import predicate) · stdlib `GoTargetOS=linux` builds green at master (battery5) |
| Host-exception ledger (i7-class) | net (DNS oracle; C#-side direct runs need `-TestTimeout 40m` here — the 10m fail is an explicit results-tail deadline kill) · crypto/tls BoGo wall — **ENCODED 2026-09-01**: the sweep now absorbs exactly the committed host-limit block on this host (400 = 3643 − 3243 disclosed, PASS host-limited; withdrawal-identity partition, five refusal controls) — no longer a sweep-form exception here · net/http's 4 h2 deadline rows (measured 250–500ms bracket) · **net/http's GO oracle itself now package-fails on this host (1,326/1,326 test-level passes, exit-time leak check) — sweep-form comparisons are unmeasurable here; the C# side proved by full direct run.** Counter-observations from G's laptop: net PASSES there, crypto/tls's BoGo FANS OUT — a banked count is host-dependent, so a red on one machine is not evidence about the row until the same host reproduces it |

## The remaining rows, by distance — re-derived 2026-09-02 (the previous table had drifted: net/http, httptrace, iter, internal/godebug, internal/concurrent, internal/poll, os/user all BANKED since it was written)

| Package | ~Verdicts | Status |
|---|---|---|
| **reflect** | ~418 | **41 rows** at the pre-hop base (R's tail: 63 → 41 in ~sixteen increments, zero regressions; union re-measure at master confirmed 42 before the sixth). The reflect `-tests` accessibility seam (i9, `54fa2b07b`) LANDED on master at `3ac4c27c8` (`734960acb`, reflect `-tests` compiles at master again); at that landing the 45 re-measures at master. Placed roots: typed-nil func type word (7+ rows — G's call-argument WIDENING, shape now stated: the parked arm's predicate + `g-nilfunc-boxing`'s `anyBoxedFuncArgs` wiring; a fourth consumer on Linux, `internal/poll` TestSplicePipePool); construction-cargo third member (2 rows, released to C2); StructOfTooLarge decoupling + GCBits (C2, sizing); TestTypes behind #34; unwrap-arm arc (R, sizing); SliceAt unsized. |
| **os** | 686 | 682/686. Two implementable blockers, neither disclosable: the NetShareAdd source-retention fork (queued, next free full lane) and the WriteStringAlloc machinery-alloc arc (ElemRefBox `(T[],nint)` increment queued). |
| **unique** | 20 | 7/20 measured. Blocker A is the type-name CARRIER arc: census + design merged (166 sites → 16 declaring / 7 Go types), A2a (carrier + field marks) cut and gated with its corpus footprint banking as surgical hunks; A2b+A3 (param 23 + result 37, one descriptor-cargo slot) sized; Stage B of the arc (the two generic sites) is what moves this row. Blocker B: 10 codegen-liveness rows, measured; banks 8 + TWELVE disclosed when A lands. |
| **runtime** | largest | `-tests` = **0 compile errors** on master; the host **survives module init** (getg no-op equivalence); the FIRST BILL prints at i9's next run (classifier landing first); next host-killer named: `TestCaller` → `moduledata.textAddr()`. |
| **runtime/pprof, runtime/trace** | medium | Capability-heavy; sequenced after runtime's bill. |
| **testing** | meta | Option 1 ruled; post-wave. |

**Linux parity axis:** 178 → **192 of 199** applicable at C1's shards 1-3 plus os/user and plugin (math/big 224+2 and net/netip 210+57 at their exact Windows counts; os/user linux: 12 behind the per-package `$cgoOffPackages` pin — confirmed by a one-variable A/B, its Windows leg PASS 5 with the pin line printing) (the roster-reader Linux fix MERGED with shard 1 -- edition-conditional reader, Desktop keeps JavaScriptSerializer, Core uses System.Text.Json; Windows 5.1 leg 540 checks + crypto/tls host-limited through the Desktop reader, both green on the i7 at the merge result).

**Darwin axis:** compiles CLEAN on both Apple runners (306/306; a 7-day silent CS0266 regression found and fixed by C2 on 2026-09-02; a daily scheduled census guards it now, and the annotation route is PROVEN AT MASTER `d56ceef6e` before the schedule's first firing: run 33580784895, osx-arm64 306/306 in 475 s, osx-x64 306/306 in 1345 s, exit 0 both -- the 2.8x x64/arm64 wall ratio is the mac legs' first calibration datum). No run layer: every program dies in `syscall.init()` → `Getrlimit` → `rawSyscall` (pinned from frames). Design record `DESIGN-darwin-run-layer.md` awaits the owner's read; recommended shape = `FuncPCABI0` + one syscall keystone family ("parallel Linux"). No Apple hardware in the fleet.

## Standing queue (in order — the pre-roll merge backlog is EMPTY, verified 2026-09-01: banking window, ctor-initializers `6e745b963`, R4 uncomparable `70b79fdf7`, symlink-privilege, splitter `507e0a4f1`, syscall-intrinsics `5bb08e9f3`, WSARecvMsg `3a3b15c21`, iter, MakeFunc/httptrace `681c71410` — all ancestors of master)
1. **The wave** (`docs/PLAN-rebank-wave.md` — the critical path to both bosses): Stage A remainder (A3/IVT merge shape pending G's CNR-at-merged-head verdict; A2 step 3 wave-gated, sizing census banked; traceback pre-pass ruled i9's), then Stage B (the seeded multi-target regen — absorbs the old queue's rebank-family and `len()`-folding items), C (classifier + doctrine amendments), D (full battery + THE full-roster sweep)
2. **runtime -tests → ZERO** (i9's residuals) → **the SEMANTIC BILL** — the campaign's headline unknown
3. **reflect tail → clean comparison** (R): InterfaceData → FuncOf → marshalCallArg←Call → shouldPanic → have/want → the singletons
4. CLAUDE.md doctrine batch — **LANDED** (`3e31de03a`, eight lessons); batch 2 accumulating on the mailbox record (warm-design trap, fan-out-not-root-pair + finding-vs-record, unsubsumed-check positive controls, the partial-seed hazard, the go-test default-wall note) — lands at G's step-3 merge or the next quiet point
5. crypto/tls LINUX annotation audit — the `linux: 400 + 2` row may be the same third host state currently passing on COUNT ALONE with none of the new evidence checked (sub-agent flag at the encoding's bank, 2026-09-01); route with the next Linux-axis slot
6. Bankable-anytime when a window opens: os (seam settled), unique (disclosure classification), os/user (denominator decision)

*History note: this tracker opened on the day the roster crossed 90% — 184 → 189 rows in one
day, with net (472, the largest networking row), reflect's first execution, and five converter/
golib/gen defect families closed. The remaining distance is 17 named rows, none a mystery.*
