# Phase 4 — The Road to 100% (living tracker)

> **LIVING coordinator-maintained surface.** Updated at every banking window; every number here
> is re-derived from [`docs/ValidatedTestPackages.md`](../ValidatedTestPackages.md) (the
> authority) and the mailbox record at update time — never carried forward. If this file and
> the roster disagree, the roster is right and this file is stale.
> Scope: the **Windows validation roster** (the 100% goal's primary axis). The per-OS parity
> axis is tracked in its own summary line below.

## Where we stand — updated 2026-09-01 morning (post-roll; successor coordinator seated)

| Measure | Value |
|---|---|
| **On master (`db67ce0fb`+ the round-6 train)** | **200 / 208 = 96.2% honest** · 27,729 matching · 154 disclosed · raw 200/215 = 93.0% — unchanged while the two bosses close: **reflect 115 → 50 real mismatches** (…PtrBytes → extendSlice → InterfaceData both halves → ChanOf/MapOf/MakeChan/Value.Close leaving the linker-record paths, the crash class HALVED 20→10 with each surviving row naming its next root), **runtime -tests 31 → 3** (CS1955's read-side alias verification landed with the CollidingPackageNames guard — the silent crypto-for-PRNG landmine defused before Stage B; Lock via the coordinator sub-agent; round 6's silent cross-scope class), remaining THREE: the traceback file alone (2 + CS8175 — ONE Stage-A pre-pass, the SEMANTIC BILL's only gate; i9 on its sizing pass now) |
| **reflect remaining distance (measured, not guessed)** | **50 at the tail-lane tip** — 42 `fail` / 7 `infrastructure-error` / 1 empty; crash class 20→7 across the arc, each survivor a NAMED root (GCBits, MakeFunc-variadic, rselect, unsafeslice, StructOfTooLarge, CallSlice ×2). typelinks fully CLOSED (contract-legal empty, non-vacuous pass stated on the record); next: marshalCallArg←Call, shouldPanic, singletons. Two rows sit on ruled/queued items rather than R's queue: TestChanOf/TestChanOfDir on the r39d narrowing (RULED: carry it — the rule's no-consumer premise died on measurement; cost canary applies), TestMapOf + TestFuncOf's checkSameType rows on the **typed-nil-func lost type word** (converter-side NilFuncValue arc, QUEUED sizing-first, now worth 2+ rows), and TestFuncOf also needs **>16-arity delegate minting** (Reflection.Emit, GoStructSynthesis's own kind — queued to the coordinator sub-agent lane ahead of ElemRefBox) |
| Shipped in 1.23.12.2 (the release snapshot) | 189 / 208 = 90.9% · 26,043 matching · 148 disclosed |
| Landed roll-day + today (every merge union-gated) | R's six reflect commits (115→63) with InterfaceData parked-with-spec · i9's residual rounds 1–3 (31→11: UntypedFloat→defined-type bridge, mixed-wrapper comparison casts, whitebox alias second-renderer, BOTH method-expression gaps + the latent cast-invoke grammar fix, anonymous-INTERFACE dedup + struct production-registry check) · G's A2 steps 1–2 + fmt-shim verb family + init-hook design record · owner-ordered session roll executed, all four successors seated or staged · **security standing order (owner): fleet nicknames only on every pushed surface — both public tips scrubbed (`a7595da67` master, `d72878d6d` mailbox), order banked in CLAUDE.md Conventions** · wave plan amended with G's A2-step-3 sizing census (2,125 hooks / 684 files / 314 pkgs; initᴛᴛtests SURVIVES, three controls; two hand-own blockers ride the relocation commit) · G's registry both-sides guard ruled (dead `reflect.methodName` registration rides it, red captured first) |
| **Rows remaining (implementable)** | **8** — reflect (55 rows: the mapped tail above, every remaining real mismatch to be rooted, fixed, or disclosed with evidence), os (682/685 measured 2026-09-01; seam SETTLED and gated, but the bank-attempt returned an honest NON-BANK — two implementable blockers, neither disclosable under standing doctrine: the NetShareAdd byte-buffer-reinterpret fork [row stays OPEN; queued for the next free full lane] and the WriteStringAlloc machinery-alloc arc [owner ruling #1: fix, never disclose; the +136 is ATTRIBUTED and decomposed to the byte — +112 the element-aliasing merge's per-box publish-gate field (correctness-load-bearing, 8 B × every ж box, proportional corpus-wide) and +24 the StringData leak-fix's window; NEITHER reverts; first increment queued: the ElemRefBox (T[],nint) ctor, which lands the row BELOW pre-regression at 16 allocs]), unique (16/20, residual isolated pure, disclosure-shaped), os/user (E2 oracle stands; ptrout members proven — bank decision grows the denominator honestly), runtime (-tests at 3; traceback cut RULED and in flight; ⚠ the bill's pipeline run is additionally gated by the step-3 -tests-manifest DUPE the canary battery caught — CS0111 duplicate initᴛᴛimport hook on x509's topology, G on it at top priority, typed-nil parked), runtime/pprof, runtime/trace, testing (Option 1 ruled, implementation sequenced) |
| In flight (all four lanes loaded) | G: step 3 AND item 4 both MERGED — the union proof delivered the FIRST three-target merged corpus building BOTH targets clean (477s/462s) with all 2,125 relocated hooks aboard; G standing by for next dispatch. R: the FuncOf three-row root next (typelinks CLOSED, 61→55 merged), then marshalCallArg←Call, shouldPanic, singletons. i9: round 6 merged (gc_test closed, the silent cross-scope class with it); next CS1955, then the traceback pre-pass sizing (likely clears all three traceback-file rows incl. CS8175). Coordinator sub-agents (i7): Lock DELIVERED and merged (mechanism: dead-since-birth registration, fixed at the real rename site, guard regressed-first); ElemRefBox increment next. Coordinator: EIGHT lane/agent merges pushed today incl. wave A2 step 3 (net −2,646 lines) + doctrine batches 1-2 (24 items); STAGE A = THE TRACEBACK PRE-PASS ALONE (i9 sizing) — Stage B window planning begins at its merge |
| Linux parity axis | 178 of 195 applicable annotated · stdlib `GoTargetOS=linux` builds green at master (battery5) |
| Host-exception ledger (i7-class) | net (DNS oracle; C#-side direct runs need `-TestTimeout 40m` here — the 10m fail is an explicit results-tail deadline kill) · crypto/tls BoGo wall — **ENCODED 2026-09-01**: the sweep now absorbs exactly the committed host-limit block on this host (400 = 3643 − 3243 disclosed, PASS host-limited; withdrawal-identity partition, five refusal controls) — no longer a sweep-form exception here · net/http's 4 h2 deadline rows (measured 250–500ms bracket) · **net/http's GO oracle itself now package-fails on this host (1,326/1,326 test-level passes, exit-time leak check) — sweep-form comparisons are unmeasurable here; the C# side proved by full direct run.** Counter-observations from G's laptop: net PASSES there, crypto/tls's BoGo FANS OUT — a banked count is host-dependent, so a red on one machine is not evidence about the row until the same host reproduces it |

## The remaining 17, by distance

### Days-class — active arcs or complete blueprints
| Package | ~Verdicts | Status |
|---|---|---|
| **net/http** | ~1,345 | **ZERO unexplained rows — bank dispatched to i9** (`claude/i9-nethttp-bank`). The 9-row model resolved: 5 confirmed by the merged fixes (union-verified); TestTransportGCRequest → **GATED under the new liveness-hang class** (fourth codegen-liveness trigger: wrapper-field round-trip, 10-variant isolation; closes OQ-L3); TestWriteDeadlineExtendedOnNewRequest/h2 → **performance-class disclosure** (bracketed: fails 125/250ms, passes 500ms; upstream carries the no-retry asymmetry). Banks after the window push, rebased + swept at real master. |
| **net/http/httptrace** | 2 | **DONE on branch — the MakeFunc chip landed** (`681c71410`: hand-owned `MakeFunc` as `Value.Call`'s exact inverse, full gate set incl. re-derived 5-row canaries at 2,247 verdicts). Banks 2\|0 at the banking window; merge owes the post-merge filtered sweep. Bonus: `reflect/iter`'s rangefunc Seq/Seq2 become live consumers. |
| **iter** | 28 | **ARC COMPLETE on branch (`claude/local-iter-arc` @`dd342ac65`): 28\|28 matching, 0 disclosed, six-run reproduced.** Managed coro rendezvous per blueprint (golib `Coro.cs` + `iter_impl.cs` via ConditionalWeakTable; Goexit/panic cross the boundary under `Goroutine.Run`); `NumGoroutine` wired — it had NEVER answered truthfully (always 1). Gates: CNR 679 byte-identical, GolibTests 434/434, converter suite, guard 4/4 twice. Banks in the post-window pass; **fully gated including the full slnx build (0 errors, 1092s — the lane's "reaped" reading was its own probe unable to observe dotnet.exe outside the worktree, corrected on the record)**; owes only bank artifacts + roster row + post-merge sweep. Bonus for the rebank family: seven pre-existing drifted runtime files censused; `gcount()` consumers chipped. |
| **internal/godebug, internal/concurrent** | small | Hand-owned-by-consequence pair; bounded local-lane bank attempts, unattempted only for lack of a slot. |
| **os** | 686 | **First-contact census DONE (08-29): 682/686 matching = 99.4%, zero unreached — the old "week-class, unattempted" label was stale history.** 4 rows: NetShareAdd declared host limit; TestDirectorySymbolicLink candidate-rooted to the `adjustTokenPrivileges` struct-passing seam (fix lane dispatched, discriminating experiment named in the census); TestWriteStringAlloc zero-bound alloc arc (improved 3,168 → ~1,185 B/op); TestUTF16Alloc disclosed, prose refresh ruled. Bankable shape once the seam row settles. Census record: `CENSUS-os-first-contact.md`. |
| **net/http/pprof, net/http/cgi** | small | Satellites: pprof rides net/http's bank; cgi is process-spawn shaped (testenv/exec plumbing now proven by debug/pe's TestDWARF). |

### Week-class — decomposing or seam-heavy
| Package | ~Verdicts | Status |
|---|---|---|
| **reflect** | ~418 | **G's arc, 192 divergences confirmed at the RC** (was will-not-compile yesterday). 125-row ABI family closed by FuncForPC; tail = the 12-test block family (platform-liveness adjacent), the named panics (`SetMapIndex`, zero-Value), the method-table identity family, DeepEqualAllocs (38, disclosure-shaped). MakeFunc chip feeds `reflect/iter` consumers. |
| **internal/syscall/windows** | medium | Seam-heavy but Windows-native; every syscall-seam remedy from the net/syscall campaigns applies. (`internal/poll` left this row 08-29: first-contact census came back **19/19 clean** — banked with the honest note that the suite covers ~7% of the FD surface; the netpoll engine is validated through the os/net rows.) |
| **unique, internal/weak, crypto/internal/boring/bcache** | small | GC-liveness territory — deliberately deferred; the codegen-liveness disclosure class and the platform-liveness residual (CoreCLR frame-slot) are the governing precedents. |

### The bosses
| Package | ~Verdicts | Status |
|---|---|---|
| **runtime** | largest | Unattempted — scheduler/GC tests meet threads-per-goroutine (SCHED-S1's measured bill) and the platform-liveness residual head-on. Expect its own campaign with a disclosure framework. |
| **runtime/pprof, runtime/trace** | medium | Capability-heavy; pprof deliberately sequenced last on the Linux frontier already. |
| **testing** | meta | The hand-owned host validating itself — special-case by design; ruling owed on what "validation" honestly means for it. |

## Standing queue (in order — the pre-roll merge backlog is EMPTY, verified 2026-09-01: banking window, ctor-initializers `6e745b963`, R4 uncomparable `70b79fdf7`, symlink-privilege, splitter `507e0a4f1`, syscall-intrinsics `5bb08e9f3`, WSARecvMsg `3a3b15c21`, iter, MakeFunc/httptrace `681c71410` — all ancestors of master)
1. **The wave** (`docs/PLAN-rebank-wave.md` — the critical path to both bosses): Stage A remainder (A3/IVT merge shape pending G's CNR-at-merged-head verdict; A2 step 3 wave-gated, sizing census banked; traceback pre-pass ruled i9's), then Stage B (the seeded multi-target regen — absorbs the old queue's rebank-family and `len()`-folding items), C (classifier + doctrine amendments), D (full battery + THE full-roster sweep)
2. **runtime -tests → ZERO** (i9's residuals) → **the SEMANTIC BILL** — the campaign's headline unknown
3. **reflect tail → clean comparison** (R): InterfaceData → FuncOf → marshalCallArg←Call → shouldPanic → have/want → the singletons
4. CLAUDE.md doctrine batch — **LANDED** (`3e31de03a`, eight lessons); batch 2 accumulating on the mailbox record (warm-design trap, fan-out-not-root-pair + finding-vs-record, unsubsumed-check positive controls, the partial-seed hazard, the go-test default-wall note) — lands at G's step-3 merge or the next quiet point
5. crypto/tls LINUX annotation audit — the `linux: 400 + 2` row may be the same third host state currently passing on COUNT ALONE with none of the new evidence checked (sub-agent flag at the encoding's bank, 2026-09-01); route with the next Linux-axis slot
6. Bankable-anytime when a window opens: os (seam settled), unique (disclosure classification), os/user (denominator decision)

*History note: this tracker opened on the day the roster crossed 90% — 184 → 189 rows in one
day, with net (472, the largest networking row), reflect's first execution, and five converter/
golib/gen defect families closed. The remaining distance is 17 named rows, none a mystery.*
